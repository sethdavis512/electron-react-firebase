import React from 'react';
import Box from '../Box';

const Header = () => {
    return (
        <Box as="header" sx={{ p: 3 }}>
            It's me the header
        </Box>
    );
};

export default Header;
